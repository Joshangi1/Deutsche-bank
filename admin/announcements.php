<?php $pageTitle='Announcements'; include __DIR__ . '/../includes/admin_header.php'; if($_SERVER['REQUEST_METHOD']==='POST'){ verify_csrf(); db()->prepare('UPDATE settings SET setting_value=? WHERE setting_key="announcement"')->execute([trim($_POST['announcement'])]); log_admin($admin['id'],'announcement','Updated announcement'); flash('success','Announcement saved.'); } $announcement=db()->query('SELECT setting_value FROM settings WHERE setting_key="announcement"')->fetch()['setting_value'] ?? ''; ?>
<form class="premium-card p-4" method="post"><?= csrf_field() ?><h5 class="fw-bold">Create announcement</h5><textarea name="announcement" class="form-control mb-3" rows="5"><?= e($announcement) ?></textarea><button class="btn btn-gold">Save announcement</button></form>
<?php include __DIR__ . '/../includes/admin_footer.php'; ?>
